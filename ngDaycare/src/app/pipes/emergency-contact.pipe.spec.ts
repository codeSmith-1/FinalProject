import { EmergencyContactPipe } from './emergency-contact.pipe';

describe('EmergencyContactPipe', () => {
  it('create an instance', () => {
    const pipe = new EmergencyContactPipe();
    expect(pipe).toBeTruthy();
  });
});
