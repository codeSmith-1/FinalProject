import { AccountRequestsPipe } from './account-requests.pipe';

describe('AccountRequestsPipe', () => {
  it('create an instance', () => {
    const pipe = new AccountRequestsPipe();
    expect(pipe).toBeTruthy();
  });
});
